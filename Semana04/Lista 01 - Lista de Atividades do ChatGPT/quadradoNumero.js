const calcularQuadrado = (num) => {
    return `O quadrado do número ${num} é igual a: ${num * num}`;
};

console.log(calcularQuadrado(4)); // Saída: 16
console.log(calcularQuadrado(7)); // Saída: 49
